// Firebase Admin SDK configuration is removed as we are switching to traditional auth.
// You can delete this file or keep it empty.

// Placeholder export to avoid breaking imports immediately (should be removed later)
export const adminAuth = null;
