// Firebase configuration is removed as we are switching to traditional auth.
// You can delete this file or keep it empty if other Firebase services might be used later.

// To completely remove Firebase dependencies, also update package.json and remove imports.

// Placeholder export to avoid breaking imports immediately (should be removed later)
export const auth = null;
export const db = null;
export const googleProvider = null;
export const app = null;
export const analytics = null;
